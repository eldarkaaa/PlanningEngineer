@model PlanningEngineerApplication.Models.Building

@{
    ViewData["Title"] = "Delete Building";
}

<h1 class="text-center">Удалить здание</h1>

<h3>Вы уверены, что хотите удалить это здание?</h3>
<div>
    <h4>@Model.Address</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Адрес
        </dt>
        <dd class="col-sm-10">
            @Model.Address
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <div class="form-group">
            <input type="submit" value="Удалить" class="btn btn-danger" /> |
            <a asp-action="Index" class="btn btn-secondary">Отменить</a>
        </div>
    </form>
</div>
